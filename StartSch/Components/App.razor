@* <html><head> stuff is in Layout/DocumentLayout.razor so that reponses to HTMX requests don't include it *@
<Router AppAssembly="typeof(Program).Assembly">
    <Found Context="routeData">
        <AuthorizeRouteView
            Resource="routeData"
            RouteData="routeData"
        />
    </Found>
</Router>
