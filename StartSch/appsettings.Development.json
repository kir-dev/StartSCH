{
    "Logging": {
        "LogLevel": {
            "Default": "Information"
        }
    },
    "ConnectionStrings": {
        "//": "Fix issue with `dotnet watch` reloading the page when the DB gets updated. Should be fixed in .NET 10",
        "Sqlite": "Data Source=../StartSch.db"
    }
}
